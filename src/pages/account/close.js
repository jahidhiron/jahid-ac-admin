import React from "react";
import CloseAccountPage from "../../components/Pages/Account/Close/Close";

const AccountClose = () => {
  return (
    <div>
      <CloseAccountPage />
    </div>
  );
};

export default AccountClose;
